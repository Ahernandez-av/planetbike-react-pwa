import styled from 'styled-components'

const Nav = styled.nav`

`

export default function Navigation() {
  return (
    <Nav>
      <div>
        <button></button>
      </div>
    </Nav>
  )
}